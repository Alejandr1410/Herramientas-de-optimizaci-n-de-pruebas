{
  "name": "qa-automation-suite",
  "version": "1.0.0",
  "description": "Suite de pruebas automatizadas - Frontend (Playwright) + Backend (Jest + Supertest)",
  "scripts": {
    "test:frontend": "npx playwright test --reporter=html",
    "test:frontend:ci": "npx playwright test --reporter=list",
    "test:backend": "npx jest backend-tests/ --forceExit --verbose",
    "test:all": "npm run test:backend && npm run test:frontend:ci"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "express": "^4.18.0"
  }
}